# huaxdamap

> Vue 3 + Vite map project

## Build Setup

```bash
# install dependencies
yarn install

# serve with hot reload
yarn dev

# build for production
yarn build

# preview production build
yarn preview
```
